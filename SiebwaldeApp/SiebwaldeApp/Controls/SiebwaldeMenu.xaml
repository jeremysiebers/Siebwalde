<UserControl x:Class="SiebwaldeApp.SiebwaldeMenu"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SiebwaldeApp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="30">

    <Grid Width="30" Background="{StaticResource WordBlueBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>


        <Button Grid.Row="1"
              Style="{StaticResource IconButton}" 
              Content="{StaticResource FontAwesomePowerOff}" 
              Command="{Binding SiebwaldeInitPage }"
              ToolTip="Communication initialization."
              Foreground="White"  
                  />

        <Button Grid.Row="2"
              Style="{StaticResource IconButton}" 
              Content="{StaticResource FontAwesomeCogIcon}" 
              Command="{Binding SiebwaldeSettingsPage }"
              ToolTip="Settings."
              Foreground="White"  
                  />
    </Grid>
</UserControl>
